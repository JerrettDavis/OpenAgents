name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Checkout AgentContainers
        uses: actions/checkout@v6
        with:
          repository: JerrettDavis/AgentContainers
          path: .agent-containers

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Setup pnpm
        uses: pnpm/action-setup@v6.0.8

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm format:check

      - name: API tests
        env:
          AGENT_CONTAINERS_PATH: ${{ github.workspace }}/.agent-containers
        run: dotnet test apps/orchestrator-api/OpenAgents.OrchestratorApi.csproj -v minimal

      - name: API coverage
        env:
          AGENT_CONTAINERS_PATH: ${{ github.workspace }}/.agent-containers
        run: pnpm test:coverage:api

      - name: Web type-check
        run: pnpm --filter orchestrator-web type-check

      - name: Web build
        run: pnpm --filter orchestrator-web build

      - name: Install Playwright browser
        run: pnpm --filter orchestrator-web exec playwright install --with-deps chromium

      - name: Web e2e
        run: pnpm --filter orchestrator-web e2e

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: |
            apps/orchestrator-web/playwright-report
            apps/orchestrator-web/test-results
          if-no-files-found: ignore

      - name: Upload API coverage
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: api-coverage
          path: apps/orchestrator-api/TestResults
          if-no-files-found: ignore
